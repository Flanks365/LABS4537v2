const msg = `Hello %s, what a beautiful day. Server time is %s`;

const miss = '404 Not Found';

const success = 'File Content Successfully Written';

const fail = ' does not exist';

module.exports = {
    msg,
    miss,
    success,
    fail
};